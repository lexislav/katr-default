{# TODO: michale, prosím dostylovat, děkuji #}

    <div class="row">
        <div class="columns small-12 ">
            <div class="menu-subsections bg-grey-light  mm-pad-bottom-half mm-pad-top-half">
            <h3 class="f-xl f-800 mm-pad-half">katalog xx prvků</h3>
                {% for subsection in entry.descendants(1) %}
                    <div class="menu-item" {% if subsection.body is empty %} class="emtpy-content" {% endif %}>
                        <a class="title" href="{{ subsection.url }}">{{ subsection.title }}</a>
                        <a class="icon" href="/polozky"><i class="fa fa-list"></i></a>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
