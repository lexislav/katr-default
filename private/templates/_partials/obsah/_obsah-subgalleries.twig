{# TODO: potřebuju rozlišit glaerii s velkými a malými náhledy, prosím doplnit třídy.
    dotaz na to je if subgallery.typGalerie == "hugeGallery" / "smallGallery"
#}

{# TODO: nastavit adekvátní verze náhledů pro tyhle galerie, zatím to jede z obecných thumb
malá galerie: ogThumbSmall
velká galerie: ogThumbLarge
 #}

{% set colSize = 'small-2' %}

{% if entry.doplkoveGalerie.typGalerie == "hugeGallery" %}
    {% set colSize = 'small-3' %}
{% endif %}

{% for subgallery in entry.doplkoveGalerie %}
    <div class="bg-grey-light mm-pad-top-half mm-pad-bottom-half">
        <div class="row">
            <div class="column small-12">
                <h2 class="f-xl f-800 mm-pad-bottom-half">{{ subgallery.galleryTitle }}</h2>
            </div>
        </div>
        <div class="row">
            {% for i in subgallery.images %}
                <div class="columns  mm-pad-bottom-half debug {{ colSize }}">
                    <div class="">
                        <img src="{{ i.getUrl('thumb') }}" alt="{{ i.filename }}">
                        <div class="">
                            {{ i.title }}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endfor %}
