{% spaceless %}
options: [ {label: '- nic -', value: null},
{% for item in library %}
    {% set cycles = craft.categories.ancestorOf(item)|length %}
    {label: '{% if cycles > 0 %}{% for i in 1..cycles %}-{% if loop.last %} {% endif %}{% endfor %}{% endif %}{{item}}', value: '{{item}}'}{% if not loop.last %},{% endif %}
{% endfor %}
],{% endspaceless %}
